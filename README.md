# Тестовое задание "Создание REST API приложения"

БД использована SQLite. предварительно заполнена данными. При желании данные можно перегенерировать

```bash
python scripts/gen_data.py
```

##### Собрать образ:
1. Убедитесь, что Python 3.11 установлен.
2. Установите зависимости:
```bash
pip install -r requirements.txt
```

3. Билд образа:
```bash
docker build -t compendium-api .
```
4. Запуск:
```bash
docker run -p 8000:8000 compendium-api
```

Перейдите в браузер, там найдется Swagger UI:
http://localhost:8000/docs

🔐 API Ключ
Для доступа ко всем эндпоинтам требуется API-ключ.

Ключ по умолчанию (в образе):
~~SUPERSECRETKeyByMax1303~~
Передайте его в заголовке:

X-API-Key: ~~SUPERSECRETKeyByMax1303~~


PS. Понимаю что было бы получше использовать PostgreSQL (как минимум было бы интереснее, что там есть PostGIS и в серьезном проекте без него бы не обошлось. Не было бы проблем с ilike/like на кириллице)

PPS. Вроде старался реализовывать всё как можно проще и сделать понятную структуру проекта

